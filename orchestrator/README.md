"""Orchestrator runtime

YAML-driven workflows, game config, and sequential agents for creator launch pipelines.

```bash
python -m orchestrator --list
python -m orchestrator gta6_news_drop --var hook="News" --var summary="Official update"
```

See `docs/architecture.md` for design details.
